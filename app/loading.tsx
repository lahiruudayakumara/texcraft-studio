import { SplashScreen } from "@/components/brand/SplashScreen";

export default function Loading() {
  return (
    <SplashScreen
      message="Loading workspace"
      detail="Bringing in your pages, project tools, and local document context."
    />
  );
}
